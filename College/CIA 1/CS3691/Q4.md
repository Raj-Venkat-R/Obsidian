---
title: 8051 Instruction Set Explanation
subject: Microcontrollers
---
# PART 1

# Instruction Set

An instruction is a command given to the computer to perform a specified operation on given data.  
The instruction set is the collection of instructions that the microcontroller is designed to execute.  
The programmer can write the program in assembly language using these instructions.

The instruction set of 8051 is classified as:
- Data transfer instructions
- Byte level logical instructions
- Bit level logical instructions
- Arithmetic instructions
- Jump and CALL instructions

# 1. Data Transfer Instructions

Immediate, direct, register and indirect addressing modes are used in different MOVE instructions.

MOV A, Rn : Copy the contents of register Rn of selected register bank to A.

MOV A, direct : Copy the contents of address specified within instruction to A.

MOV A, @Ri : Copy the contents of the address in Ri to A.

MOV A, #data : Load the data given in the instruction to A.

### Instructions to Access External Data Memory

MOVX A, @Ri : Copy the contents of the external address in Ri to A.

MOVX A, @DPTR : This instruction copies data from the 16-bit address in DPTR to A.

### Instructions to Access Program Memory / External ROM

MOVC A, @A+DPTR : Copy the contents of the external ROM address formed by adding A and the DPTR to A.

MOVC A, @A+PC : Copy the contents of the external ROM address formed by adding A and the PC to A.

Important points to remember in accessing external ROM:
- When PC is used to access external ROM, it is incremented by 1 before it is added to A to form the physical address of external ROM.
- All external data moves with external ROM involve the A register.
- The DPTR and the PC are not changed.

---

# 2. Arithmetic Instructions

The arithmetic operations of 8051 include increment, decrement, addition, subtraction, multiplication, division and decimal operations.

## Incrementing and Decrementing

Incrementing and decrementing instructions allow addition and subtraction of 1 from a given number.  
These instructions do not affect C, AC and OV flags.

INC A : Increment accumulator by 1  
INC Rn : Increment register  
INC direct : Increment direct byte  
INC @Ri : Increment indirect RAM  

DEC A : Decrement accumulator by 1  
DEC Rn : Decrement register  
DEC direct : Decrement direct byte  
DEC @Ri : Decrement indirect RAM  

## Addition

ADD A, Rn : Add register to accumulator  
ADD A, direct : Add direct byte to accumulator  

ADDC – Add with carry  

ADDC A, @Ri : Add indirect RAM to A with carry flag  
ADDC A, #data : Add immediate data to A with carry flag  

## Subtraction

SUBB A, Rn : Subtract register from A with borrow  
SUBB A, direct : Subtract direct byte from A with borrow  
SUBB A, @Ri : Subtract indirect RAM from A with borrow  
SUBB A, #data : Subtract immediate data from A with borrow  

## Multiplication and Division

MUL AB : Multiplies the unsigned eight-bit integers in the accumulator and register B.

DIV AB : Divides the unsigned eight-bit integer in the accumulator by the unsigned eight-bit integer in register B.

---

# 3. Logical Instructions

## Byte Level Logical Instructions

The instructions ANL, ORL and XRL perform the logical functions AND, OR and Exclusive-OR on the two byte variables indicated, leaving the results in the first. No flags are affected.

ANL A, Rn : AND register to accumulator  
ANL A, direct : AND direct byte to accumulator  
ANL A, #data : AND immediate data to accumulator  

ORL A, Rn : OR register to accumulator  
ORL A, @Ri : OR indirect RAM to accumulator  

XRL A, Rn : Exclusive-OR register to accumulator  
XRL A, direct : Exclusive-OR direct byte to accumulator  
XRL A, @Ri : Exclusive-OR indirect RAM to accumulator  

---

# 4. Data Exchange Instructions

The exchange instructions move data from the source address to destination address and vice versa.

XCH A, Rn : Exchange data bytes between register Rn and A.

XCH A, direct : Exchange data bytes between address directly given within instruction and A.

Important points to remember in exchange instructions:
- All exchanges involve the A register.
- All exchanges take place internally within 8051.
- Immediate addressing mode cannot be used in the exchange instructions.
- When XCHD A, @Ri instruction is executed, the upper nibble of A and the upper nibble of the address in Ri do not change.

---

# PART 2

# 5. Push and Pop Instructions

## Data Transfer with Stack Instructions

PUSH B : Increments the SP by one and stores the contents of register B.

POP ACC : Copies the contents of location addressed by the SP to the accumulator. SP is decremented by one.

---

# 6. Rotate and Swap Instructions

RL A : Rotate accumulator left.  
The eight bits in the accumulator are rotated one bit to the left. Bit 7 is rotated into the bit 0 position. No flags are affected.

RLC A : Rotate A left through the carry flag.  
The eight bits in the accumulator and the carry flag are together rotated one bit to the left.  
Bit 7 moves into the carry flag; the original state of the carry flag moves into the bit 0 position.

RR A : Rotate accumulator right.  
The eight bits in the accumulator are rotated one bit to the right. Bit 0 is rotated into the bit 7 position. No flags are affected.

RRC A : Rotate A right through carry flag.  
The eight bits in the accumulator and the carry flag are together rotated one bit to the right.  
Bit 0 moves into the carry flag; the original value of carry flag moves into the bit 7 position.

SWAP A : Swap nibbles within the accumulator.  
SWAP A interchanges the low and high-order nibbles of the accumulator. The operation can also be thought of as a four-bit rotate instruction. No flags are affected.

---

# 7. Bit Oriented Instructions

Bit level manipulations are very convenient when it is necessary to set or reset a particular bit in the internal RAM or SFRs.

The internal RAM of 8051 from address 20H through 2FH is both byte addressable and bit addressable.

CLR C : Clear carry flag  
CLR bit : Clear direct bit  

SETB C : Set carry flag  
SETB bit : Set direct bit  

ANL C, bit : AND direct bit to carry flag  
ANL C, /bit : AND complement of direct bit to carry  

ORL C, bit : OR direct bit to carry flag  
ORL C, /bit : OR complement of direct bit to carry  

CPL C : Complement carry flag  
CPL bit : Complement direct bit  

---

# 8. Branch Instructions

Jump and CALL instructions change the flow of the program by changing the contents of program counter.

Jump permanently changes the program flow whereas Call temporarily changes the program flow to allow another part of the program to run.

There are jump instructions which change the program flow if certain condition exists.

Example:
CJNE – Compare and Jump if not equal

The following types of instructions change the program flow:
- Jump on bit conditions
- Compare byte and jump if not equal
- Decrement byte and jump if zero
- Jump unconditionally
- Call a subroutine
- Return from a subroutine

AJMP addr11 : Absolute Jump.  
AJMP transfers program execution to the indicated address. Since address is 11-bit, the destination must therefore be within the same 2K block of program memory.

LJMP addr16 : Long Jump.  
LJMP causes an unconditional branch to the indicated address by loading the PC. The destination may therefore be anywhere in the full 64K program memory.

JMP @A+DPTR : Jump indirect relative to the DPTR.

JZ rel : Jump if accumulator is zero.  
JNZ rel : Jump if accumulator is not zero.  
JC : Jump if carry flag is set.  
JNC : Jump if no carry flag.  
NOP : No operation.

## CALL and Subroutines

There are two subroutine call instructions:
LCALL (Long call) and ACALL (Absolute call).

The generic form of the call operation is the mnemonic CALL, which 8051 will translate into LCALL or ACALL as appropriate.

---
